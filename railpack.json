{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "20"
  },
  "steps": {
    "install": {
      "commands": [
        "echo 'No install needed — landing page is zero-dependency.'"
      ]
    },
    "build": {
      "commands": [
        "echo 'Nothing to build — static HTML served by landing/server.js.'"
      ]
    }
  },
  "deploy": {
    "startCommand": "node landing/server.js"
  }
}
